class A {
  constructor(foo, bar, lorem) {
    this.foo = foo;
    this.bar = bar;
    this.lorem = lorem; // Nuevo atributo
  }
}

// Exporta la clase A para poder usarla en otros archivos
module.exports = A;
